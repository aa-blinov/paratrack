{
  "name": "paratrack-ui",
  "private": true,
  "version": "0.0.0",
  "description": "paratrack embedded UI source — Tailwind v4 + DaisyUI v5 build pipeline",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ../internal/web/static/css/paratrack.css --minify",
    "watch": "tailwindcss -i ./input.css -o ../internal/web/static/css/paratrack.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "daisyui": "^5.7.43",
    "tailwindcss": "^4.3.3"
  }
}
